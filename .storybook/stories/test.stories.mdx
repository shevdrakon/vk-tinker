import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import {Button} from '@storybook/react/demo';

<Meta title="Demo/Badge" component={Button} />

# Badge

With `MDX` we write longform markdown documentation for our `Button` component and embed Doc Blocks inline.

<Props of={Button} />

<Story name="positive">
  <Button status="positive">Positive</Button>
</Story>
